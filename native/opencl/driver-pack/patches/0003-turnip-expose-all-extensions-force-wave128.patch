--- a/src/vulkan/util/vk_extensions.py
+++ b/src/vulkan/util/vk_extensions.py
@@ -50,6 +50,8 @@ class Extension:
         return ext
 
     def c_android_condition(self):
+        # TQ: Expose ALL Vulkan extensions on Android (adrenotools pattern)
+        return 'true'
         # if it's an EXT or vendor extension, it's allowed
         if not self.name.startswith(ANDROID_EXTENSION_WHITELIST_PREFIXES):
             return 'true'
--- a/src/freedreno/vulkan/tu_device.cc
+++ b/src/freedreno/vulkan/tu_device.cc
@@ -1551,7 +1551,8 @@ tu_physical_device_init(struct tu_physic
 
    switch (fd_dev_gen(&device->dev_id)) {
    case 6:
-   case 7: {
+   case 7:
+   case 8: {
       device->dev_info = info;
       device->info = &device->dev_info;
       uint32_t depth_cache_size =
+
+/*
+ * NOTE:
+ * Wave/subgroup policy is intentionally NOT expressed here as a synthetic
+ * tu_shader.cc pseudo patch anymore. The authoritative implementation lives in
+ * the recovered Mesa source tree plus runtime tuning and forensic artifacts.
+ * This patch now carries only reproducible diff hunks.
+ */
